extends layouts/main.pug
include mixins/videoBlock.pug

block contents
    .videos
        each item in videoDB
             +videoBlock({
                 id:item.id,
                 title : item.title,
                 views : item.views,
                 videoFile: item.videoFile
             })