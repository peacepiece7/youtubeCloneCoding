extends layouts/main.pug
include mixins/videoBlock.pug

block contents
    .videos
        each item in videoDB
             +videoBlock({
                 title : item.title,
                 views : item.views,
                 videoFile: item.videoFile
             })